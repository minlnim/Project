# Backend 환경 변수 - PRODUCTION

# 기본
NODE_ENV=production
PORT=8080

# AWS / Cognito (Terraform Output 값)
AWS_REGION=ap-northeast-1
COGNITO_USER_POOL_ID=ap-northeast-1_XXXXXXXXX
COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# RDS PostgreSQL (Terraform Output 값)
DB_HOST=tokyo-portal-tokyo-db.xxxxxxxx.ap-northeast-1.rds.amazonaws.com
DB_PORT=5432
DB_USER=admin
DB_PASS=your-secure-password-from-terraform-tfvars
DB_NAME=corpportal

# ⚠️ 중요: DB_PASS를 terraform.tfvars의 db_password 값으로 변경하세요!
