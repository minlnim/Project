# NOTE: default namespace를 사용하므로 이 파일은 필요하지 않습니다.
# ArgoCD가 이 파일을 무시하도록 주석 처리되었습니다.
# 만약 별도의 namespace를 사용하려면 주석을 해제하고
# deployment.yaml, service.yaml, configmap.yaml, secret.yaml의
# namespace 필드도 함께 수정해야 합니다.

# apiVersion: v1
# kind: Namespace
# metadata:
#   name: tokyo-portal
#   labels:
#     name: tokyo-portal
